import { createTheme } from "@mui/material/styles";

// Export the default MUI theme without any customization.
// Note: The application is not wrapped in a ThemeProvider; this is provided only
// for tests or utilities that may need a theme instance directly.
export const theme = createTheme();
